#include<stdio.h>
#include<stdlib.h>

int main(){
    system("clear");
    // int num = 1, sum = 0;
    // printf("Enter the number: ");
    // scanf("%d", &num);
    int i=0;
    for( ; ; ){
        printf("%d \n", i++);
    }
    return 0;
}